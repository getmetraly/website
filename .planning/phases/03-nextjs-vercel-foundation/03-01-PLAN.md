---
phase: 03-nextjs-vercel-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
  - tsconfig.json
  - app/layout.tsx
  - app/globals.css
  - app/page.tsx
  - components/site-shell.tsx
  - components/site-nav.tsx
  - components/site-footer.tsx
  - components/theme-provider.tsx
  - components/theme-toggle.tsx
requirements: [NEXT-01, NEXT-03, NEXT-04]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Repository has a real Next.js App Router scaffold."
    - "Shared shell, theme, nav, and footer primitives exist before page migration."
    - "Local scripts use next dev/build/start."
  artifacts:
    - package.json
    - app/layout.tsx
    - app/globals.css
    - components/site-shell.tsx
  key_links:
    - "Next.js App Router root layout pattern from /vercel/next.js docs."
---

<objective>
Create the Next.js application foundation without changing product copy or implementing the full demo.
</objective>

<tasks>
- Add Next.js, React, React DOM, TypeScript, and minimal lint/build tooling in `package.json`.
- Generate/update lockfile through the package manager used for installation.
- Add `dev`, `build`, `start`, and `lint` scripts.
- Add `app/layout.tsx` with root `<html>` and `<body>` wrapper, metadata, theme bootstrap, and global stylesheet import.
- Add `app/globals.css` by extracting shared tokens and base styles from the current prototype.
- Add reusable site shell components for navigation, footer, theme provider, and theme toggle.
- Add a temporary `app/page.tsx` route that renders the current home page shell or a minimal migrated home route so `next build` has an entry point.
</tasks>

<verification>
- `test -f package.json`
- `test -f app/layout.tsx`
- `test -f app/page.tsx`
- `test -f components/site-nav.tsx`
- `rg -n "\"dev\": \"next dev\"|\"build\": \"next build\"|\"start\": \"next start\"" package.json`
- `rg -n "export default function RootLayout|<html|<body" app/layout.tsx`
</verification>

<success_criteria>
- Next.js scaffold exists and is ready for page migration.
- Shared shell avoids duplicating navigation/footer/theme code in every page.
- No Phase 2 canonical copy is intentionally changed in this scaffold task.
</success_criteria>

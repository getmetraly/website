---
phase: 03-nextjs-vercel-foundation
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - next.config.ts
  - package.json
  - README.md
  - .gitignore
  - .planning/phases/03-nextjs-vercel-foundation/03-03-SUMMARY.md
requirements: [NEXT-02, NEXT-04, NEXT-05]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Vercel can build the site with npm install and npm run build."
    - "Old .html entry points redirect to new route paths."
    - "Build does not require secrets, backend services, or ../app."
  artifacts:
    - next.config.ts
    - package.json
    - README.md
  key_links:
    - "Vercel deploy target is the root of the website repository."
---

<objective>
Make the migrated Next.js site buildable and deployable on Vercel with route compatibility and a documented local workflow.
</objective>

<tasks>
- Add `next.config.ts` redirects for old `.html` paths to the new App Router paths.
- Add or update `.gitignore` for `.next`, `node_modules`, Vercel local files, and build artifacts.
- Add `README.md` with local setup, build, and Vercel deployment notes.
- Run dependency install if needed to create/update `package-lock.json`.
- Run `npm run build` and fix any migration errors.
- Run stale-copy checks from Phase 2 against migrated `app/` routes.
- Run route/link smoke checks using static search at minimum; if a dev server is running, verify core routes in browser.
</tasks>

<verification>
- `npm run build`
- `rg -n "source: '/(index|pricing|docs|login|privacy|terms)\\.html'" next.config.ts`
- `rg -n "vercel|Vercel|npm run build|npm run dev" README.md`
- `! rg -n "FSL|Functional Source|source-available|Apache 2.0|FSL License|Live Demo Instance|Data resets every 24h|SOC 2 auditors|AWS GovCloud" app components`
- `! rg -n "process\\.env\\.[A-Z0-9_]+" app components next.config.ts`
</verification>

<success_criteria>
- `npm run build` succeeds.
- Vercel deploy path is documented and does not need custom infrastructure.
- Legacy `.html` URLs have explicit redirects.
- The migration does not reintroduce stale license, pricing, or demo claims.
</success_criteria>

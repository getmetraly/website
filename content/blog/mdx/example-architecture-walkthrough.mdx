export const metadata = {
  title: 'Architecture walkthrough example',
  description: 'Example MDX article for future deep technical writeups.',
}

# Architecture walkthrough

This article demonstrates the future MDX publishing flow for Metraly.

## Why MDX matters

MDX allows engineering articles to combine:

- prose
- code examples
- diagrams
- architecture explanations
- interactive components

> Technical content should feel like engineering documentation, not only marketing copy.

## Example code block

```ts
export function evaluateDeliveryRisk(signal: number) {
  return signal > 0.8 ? 'high' : 'normal'
}
```

## Future embedded systems

The next layer can add:

- architecture diagrams
- interactive dashboards
- benchmark visualizations
- embedded demos

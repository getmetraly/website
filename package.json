{
  "name": "metraly-website",
  "version": "0.1.0-alpha",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "lint": "npm run lint:claims && npm run lint:code",
    "lint:claims": "node scripts/check-forbidden-claims.mjs",
    "lint:code": "node scripts/run-eslint.mjs",
    "smoke:routes": "node scripts/smoke-routes.mjs",
    "smoke:e2e": "npx --yes --package=@playwright/test@1.57.0 playwright test",
    "audit:deps": "npm audit --omit=dev --audit-level=high",
    "ci": "npm run lint && npm run typecheck && npm run build"
  },
  "dependencies": {
    "next": "^15.5.15",
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "typescript": "^5.4.0"
  }
}

name: Website CI

on:
  workflow_dispatch:

jobs:
  build-and-smoke:
    name: Build and smoke test
    runs-on: ubuntu-latest

    steps:
      - name: CI temporarily disabled
        run: echo "Website CI is temporarily disabled during tooling and UI iteration"
